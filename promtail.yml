server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: ${lokiURL}/loki/api/v1/push

scrape_configs:
# Logs sistema
- job_name: MiPC
  static_configs:
  - targets:
      - MIPC
    labels:
      job: system
      name: miPc
      __path__: /var/log/*log