server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: ${lokiURL}/loki/api/v1/push

scrape_configs:
# Logs sistema
- job_name: Pre
  static_configs:
  - targets:
      - Pre
    labels:
      job: tomcat-8036s
      name: Preprod
      #__path__: /home_server/javateam/logsTestBorrar/*/*log
      __path__: ${tomcat-8036}
