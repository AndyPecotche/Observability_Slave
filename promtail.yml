server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: ${lokiURL}/loki/api/v1/push

scrape_configs:
# Logs sistema
- job_name: Pre
  static_configs:
  - targets:
      - localhost
    labels:
      job: logst11
      name: l11
      #hostname: ${hostname}
      __path__: /var/logst1/*log
  - targets:
      - localhost
    labels:
      job: logst22
      name: l23
      #hostname: ${hostname}
      __path__: /var/logst2/*log
